# HCL-Github-Actions-Helm
This repo is for github actions along with helm chart.

## K8s folder is not used in these project, because we use helm folder instead of k8s and updating the values.yaml instead of k8s deployment yaml.
## How K8s folder came into the project: because it is a repo from the hcl+githubactions+k8s. we used that code as a source code for these repo.


